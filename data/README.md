To be able to use the solver program, you first need to compute the interaction matrix with

```
bzipscore-all.py full4096.fasta > full4069.out
```

Then, you can run the solver, with

```
solver full4096.out 4096
```